#!/usr/bin/env bash

script_root_dir="$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
PROJECT_DIR="$(dirname $script_root_dir)"

docker-compose -f "$PROJECT_DIR/docker-compose.yml" -p geolocation --project-directory $PROJECT_DIR exec web bundle exec rails c
